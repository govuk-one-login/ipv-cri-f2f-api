name: Deploy PostOffice Mock

on:
  push:
    branches:
      - main
    paths:
      - 'post-office-stub/**'
  workflow_dispatch:
    inputs:
      environment:
        description: 'Deploy to which environment?'
        required: true
        type: choice
        options:
          - DEV
          - BUILD

permissions:
  id-token: write
  contents: read

env:
  AWS_REGION: eu-west-2

jobs:
  setup:
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    outputs:
      environments: '["DEV", "BUILD"]'
    steps:
      - run: echo "Setting up environments"

  deploy:
    name: Validate & Deploy PostOffice Mock template
    runs-on: ubuntu-latest
    needs: setup
    defaults:
      run:
        working-directory: post-office-stub
    strategy:
      matrix:
        environment: ${{ fromJson(needs.setup.outputs.environments) }}
    steps:
      - name: Check out repository code
        uses: actions/checkout@v4

      - name: Setup SAM CLI
        uses: aws-actions/setup-sam@v2
        with:
          use-installer: true

      - name: Assume temporary AWS role
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets[format('{0}_PO_MOCK_GH_ACTIONS_ROLE_ARN', matrix.environment)] }}
          aws-region: ${{ env.AWS_REGION }}

      - name: SAM Validate
        run: sam validate --region ${{ env.AWS_REGION }} -t template.yaml

      - name: SAM Build
        run: sam build -t template.yaml

      - name: Deploy SAM app
        uses: alphagov/di-devplatform-upload-action@v3
        with:
            artifact-bucket-name: ${{ secrets[format('{0}_PO_MOCK_ARTIFACT_SOURCE_BUCKET_NAME', matrix.environment)] }}
            signing-profile-name: ${{ secrets[format('{0}_CRI_F2F_SIGNING_PROFILE_NAME', matrix.environment)] }}
            working-directory: post-office-stub
            template-file: template.yaml
